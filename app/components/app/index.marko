
class {
    onCreate() {
        this.state = {
            mounted: false
        };
    }
    onMount() {
        this.state.mounted = true;
    }
}

<div>
    <p if(state.mounted)>
        UI component successfully mounted!s
    </p>

<!-- call component! -->
    <click-count/>
</div>